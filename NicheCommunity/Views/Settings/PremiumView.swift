//
//  PremiumView.swift
//  NicheCommunity
//
//  프리미엄 뷰 — 제거됨 (모든 기능 무료 해제)
//

import SwiftUI

/// 프리미엄 뷰 (사용하지 않음 — 모든 기능이 무료 해제됨)
struct PremiumView: View {
    @Environment(\.dismiss) private var dismiss

    var body: some View {
        EmptyView()
    }
}
